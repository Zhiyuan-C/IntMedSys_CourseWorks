<figure class="recentThumb" modalUrl="{{modal.imageSrc}}" modalProfile="{{modal.href}}" modalOwIcon="{{modal.iconSrc}}" modalCaption="{{modal.caption}}" recentThumb="{{modal.recentThumb}}">
    <img src="{{modal.recentThumb}}" />
</figure>